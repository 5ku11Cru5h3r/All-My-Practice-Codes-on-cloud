#include <bits/stdc++.h>
using namespace std;
int main(){
    int n,m,k;
    cin >> n >>m>>k;
    vector<int> v(n),vv(m);
    auto print =[](const int &n){cout<< n <<endl;};
    for (auto &&i : v)
    {
        cin >> i;
    }
    for (auto &&i : v)
    {
        cin>>i;
    }
    
    
}